/*
 * Artifacts API
 *
 *  Artifacts is an API-based MMO game where you can manage 5 characters to explore, fight, gather resources, craft items and much more.  Website: https://artifactsmmo.com/  Documentation: https://docs.artifactsmmo.com/  OpenAPI Spec: https://api.artifactsmmo.com/openapi.json 
 *
 * The version of the OpenAPI document: 6.1.0
 * Generated by: https://github.com/openapitools/openapi-generator.git
 */


using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Linq;
using System.IO;
using System.Runtime.Serialization;
using System.Text;
using System.Text.RegularExpressions;
using Newtonsoft.Json;
using Newtonsoft.Json.Converters;
using Newtonsoft.Json.Linq;
using System.ComponentModel.DataAnnotations;
using FileParameter = ArtifactsMmoClient.Client.FileParameter;
using OpenAPIDateConverter = ArtifactsMmoClient.Client.OpenAPIDateConverter;

namespace ArtifactsMmoClient.Model
{
    /// <summary>
    /// ChangePassword
    /// </summary>
    [DataContract(Name = "ChangePassword")]
    public partial class ChangePassword : IValidatableObject
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="ChangePassword" /> class.
        /// </summary>
        [JsonConstructorAttribute]
        protected ChangePassword()
        {
            this.AdditionalProperties = new Dictionary<string, object>();
        }
        /// <summary>
        /// Initializes a new instance of the <see cref="ChangePassword" /> class.
        /// </summary>
        /// <param name="currentPassword">Your password. (required).</param>
        /// <param name="newPassword">New password. (required).</param>
        public ChangePassword(string currentPassword = default, string newPassword = default)
        {
            // to ensure "currentPassword" is required (not null)
            if (currentPassword == null)
            {
                throw new ArgumentNullException("currentPassword is a required property for ChangePassword and cannot be null");
            }
            this.CurrentPassword = currentPassword;
            // to ensure "newPassword" is required (not null)
            if (newPassword == null)
            {
                throw new ArgumentNullException("newPassword is a required property for ChangePassword and cannot be null");
            }
            this.NewPassword = newPassword;
            this.AdditionalProperties = new Dictionary<string, object>();
        }

        /// <summary>
        /// Your password.
        /// </summary>
        /// <value>Your password.</value>
        [DataMember(Name = "current_password", IsRequired = true, EmitDefaultValue = true)]
        public string CurrentPassword { get; set; }

        /// <summary>
        /// New password.
        /// </summary>
        /// <value>New password.</value>
        [DataMember(Name = "new_password", IsRequired = true, EmitDefaultValue = true)]
        public string NewPassword { get; set; }

        /// <summary>
        /// Gets or Sets additional properties
        /// </summary>
        [JsonExtensionData]
        public IDictionary<string, object> AdditionalProperties { get; set; }

        /// <summary>
        /// Returns the string presentation of the object
        /// </summary>
        /// <returns>String presentation of the object</returns>
        public override string ToString()
        {
            StringBuilder sb = new StringBuilder();
            sb.Append("class ChangePassword {\n");
            sb.Append("  CurrentPassword: ").Append(CurrentPassword).Append("\n");
            sb.Append("  NewPassword: ").Append(NewPassword).Append("\n");
            sb.Append("  AdditionalProperties: ").Append(AdditionalProperties).Append("\n");
            sb.Append("}\n");
            return sb.ToString();
        }

        /// <summary>
        /// Returns the JSON string presentation of the object
        /// </summary>
        /// <returns>JSON string presentation of the object</returns>
        public virtual string ToJson()
        {
            return Newtonsoft.Json.JsonConvert.SerializeObject(this, Newtonsoft.Json.Formatting.Indented);
        }

        /// <summary>
        /// To validate all properties of the instance
        /// </summary>
        /// <param name="validationContext">Validation context</param>
        /// <returns>Validation Result</returns>
        IEnumerable<ValidationResult> IValidatableObject.Validate(ValidationContext validationContext)
        {
            // CurrentPassword (string) maxLength
            if (this.CurrentPassword != null && this.CurrentPassword.Length > 50)
            {
                yield return new ValidationResult("Invalid value for CurrentPassword, length must be less than 50.", new [] { "CurrentPassword" });
            }

            // CurrentPassword (string) minLength
            if (this.CurrentPassword != null && this.CurrentPassword.Length < 5)
            {
                yield return new ValidationResult("Invalid value for CurrentPassword, length must be greater than 5.", new [] { "CurrentPassword" });
            }

            if (this.CurrentPassword != null) {
                // CurrentPassword (string) pattern
                Regex regexCurrentPassword = new Regex(@"^[^\s]+$", RegexOptions.CultureInvariant);
                if (!regexCurrentPassword.Match(this.CurrentPassword).Success)
                {
                    yield return new System.ComponentModel.DataAnnotations.ValidationResult("Invalid value for CurrentPassword, must match a pattern of " + regexCurrentPassword, new [] { "CurrentPassword" });
                }
            }

            // NewPassword (string) maxLength
            if (this.NewPassword != null && this.NewPassword.Length > 50)
            {
                yield return new ValidationResult("Invalid value for NewPassword, length must be less than 50.", new [] { "NewPassword" });
            }

            // NewPassword (string) minLength
            if (this.NewPassword != null && this.NewPassword.Length < 5)
            {
                yield return new ValidationResult("Invalid value for NewPassword, length must be greater than 5.", new [] { "NewPassword" });
            }

            if (this.NewPassword != null) {
                // NewPassword (string) pattern
                Regex regexNewPassword = new Regex(@"^[^\s]+$", RegexOptions.CultureInvariant);
                if (!regexNewPassword.Match(this.NewPassword).Success)
                {
                    yield return new System.ComponentModel.DataAnnotations.ValidationResult("Invalid value for NewPassword, must match a pattern of " + regexNewPassword, new [] { "NewPassword" });
                }
            }

            yield break;
        }
    }

}
